class CardItem {
  constructor(quantity, productPrice, title, sum) {
    this.quantity = quantity;
    this.productPrice = productPrice;
    this.title = title;
    this.sum = sum;
  }
}
export default CardItem;
